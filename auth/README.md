Files related to authentication with external services.

The files in this directory are excluded by default from git commits via `.gitignore`. Any files that need to be committed must be approved by NMDC team members and explicitly added as exceptions to the `.gitignore` file.
